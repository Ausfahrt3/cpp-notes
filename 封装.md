# 初始化列表

必须使用初始化列表的三种情况
- 引用成员变量：引用在定义时必须进行初始化，而初始化列表能够满足需求，在函数体内只是赋值操作
- const变量
- 无默认构造函数的自定义类型：成员对象无默认构造函数时，需要在构造函数中进行初始化列表进行初始化
因此，最好都在初始化列表中进行初始化
注意⚠️
- 初始化顺序是成员变量在类中声明的次序，与列表中的书写顺序无关
- 初始化列表中顺序最好与声明次序
- 禁止用一个变量初始化另一个变量
# explicit关键字：防止隐式类型转换

建议单参数构造函数添加explicit关键字，因为单参数的支持隐式类型转换
# 隐式类型转换原理

先构造临时对象，再
# static成员

- 适用计数器，全局配置，工具函数等
# 变量

- 共享
- 存储在静态区，不占用对象空间
- 类外初始化
# 函数

- 没有this，所以没有与对象绑定，所以不能访问非静态
- 只能访问静态成员
- 可直接通过类名调用，即“类名：：函数名”
- 与非静态成员函数相比，虽然非静态成员函数也是共享的，但其有this指针，与对象绑定了
## 访问：

共有时，推荐通过类名访问，因为这样体现出其属于类，而非对象
- 匿名对象访问（只用一次，以后再也不用，为减少命名空间资源浪费），一步即可，不需再定义变量
- 类对象访问
- 类名访问
私有时，需要公共接口访问，方法依旧以上三种

非静态可在成员声明时给缺省值，即int a=10
静态不能，因为必须类外初始化
# 友元函数

全局函数无法访问私有成员，使用友元函数可以突破封装
- 可直接访问类的私有成员
- 定义在类外部的普通函数
- 不属于任何类，不是成员函数
- 需在类内声明，并加上friend关键字
# 友元类

友元类可以访问另一个类的所有成员，但这种关系是单向声明（B是A的友元，但A不是B的友元），并且不可传递
# 内部类

- 一个类定义在另一个类的内部，可以定义在类内的任何区域
- 内部类是一个独立的类，不是组成关系
- 不能通过外部类的对象去调用内部类，因为独立
- **内部类可以通过外部类的对象参数访问外部类的所有成员**，即内部可以访问外部（注意加作用域运算符），但外部不可访问内部，相当于内部类是外部类的友元类
# 单例模式

