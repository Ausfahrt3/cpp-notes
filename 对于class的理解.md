#date：2026/4/10
#lable：实验2思考题
# 类=数据+行为

## 类的数据，即所包含的变量
### 普通变量

- 在类中设定其属性，即这个类有什么东西，如一个点类，在类中设定x,y变量，那么即成一个类，然后使用该类组成一个对象，一个实例。如：class Center，即是一个点的对象，该对象包含(x,y)的点。
- 在类中可以存在如上文提到的class Center的对象——用已有的类，组合出更复杂的类。

**注意：使用对象访问成员，使用"."运算符，即”Center.x“，对象不是指针。**
### 静态变量

- 属于类，不属于对象，所有对象共享一份
- 程序一开始运行，静态变量就已经存在了,就算一个对象都不创建，静态变量也在内存中存在
- 必须在类外初始化
- 可以通过类名 :: 直接访问
```C++
class CStudent
{
private:
    static int nTotalObj;  // 类内声明
};

int CStudent::nTotalObj = 0; // 类外初始化
```
### 成员对象
```C++
class CStudent {
    // 有带参构造，没有默认构造
    CStudent(char* n, int a);
};

class CTeacher {
private:
    // stu 是 CTeacher 的「成员对象」
    CStudent stu;  //自动调用无参默认构造
};
```
- **成员对象没有默认构造时，只能用初始化列表初始化**（在调用成员对象的构造函数中进行初始化）
```C++
CTeacher::CTeacher(char *tn, int ta)
    : stu("小明", 18)   // 初始化列表：直接给 stu 调用带参构造
{
    // 函数体里这时 stu 已经初始化完了
}
```
- 初始化列表**可以直接调用另一个类的带参构造**
- 构造函数 `{}` 里面**不能再重新初始化成员对象**，只能赋值
## 类的行为，即所包含的函数

- 此函数即类能使用自己的数据所能执行的”动作“。
- 在类内的函数说明这个类中的数据拥有这种功能，如提到的点类”class CPoint“，在其中设定”判断‘数据’（即封装后的对象）是否位于圆内“的函数，则说明Center这个点拥有这个判断功能。
### 内部函数与外部函数

- **在一个类里面写的成员函数 = 内部函数**
- **不在这个类里面的所有函数 = 外部函数**
```C++
class CStudent
{
private:
    int age;   // 私有成员
public:
    // 这些都属于 CStudent 的【内部成员函数】
    void fun1();
};

class CTeacher
{
public:
    // 这个函数属于 CTeacher 的函数
    void SetStuAge(int a);  
};
```
**对于CStudent来说：** 

- **CStudent 自己里面的成员函数** → 内部，可以随便访问 `private` 的 age
- **CTeacher 里的成员函数** → **属于外部函数**
- **main 主函数** → 外部函数
- **全局普通函数** → 外部函数

**所以**：

`CTeacher` 的 `SetStuAge()`
**相对于 CStudent 是外部函数**
```C++
stu.age = a;   // 报错！age 是 private，外部无权碰
```
**解决**：
依靠**公有接口**在 CStudent 里给个 public 函数，实现外部更改private：
```C++
public:
    void SetAge(int a)
    {
        age = a;
    }
```
然后外部只能这样用：
```C++
stu.SetAge(a);  // ✅ 合法
```
### [[class 的特殊函数]]
### 静态成员函数

- 属于类，没有 this 指针
- 只能访问静态成员变量 / 静态成员函数，不能访问非静态成员
- 调用方式：类名::静态函数名()，用对象调用（也可以，但不推荐）
- 与非静态成员函数相比，虽然非静态成员函数也是共享的，但其有this指针，与对象绑定了
#### 访问：

共有时，推荐通过类名访问，因为这样体现出其属于类，而非对象
- 匿名对象访问（只用一次，以后再也不用，为减少命名空间资源浪费），一步即可，不需再定义变量
- 类对象访问
- 类名访问
私有时，需要公共接口访问，方法依旧以上三种

非静态可在成员声明时给缺省值，即int a=10
静态不能，因为必须类外初始化
### 友元函数

全局函数无法访问私有成员，使用友元函数可以突破封装
- 可直接访问类的私有成员
- 定义在类外部的普通函数
- 不属于任何类，不是成员函数
- 需在类内声明，并加上friend关键字
### 类内函数的this指针

- this 是类成员函数里的一个**隐藏的，自动存在的**指针，指向这个被调用函数所需要的数据（类内未被封装的数据）本身。
- 类内函数不用传点，也能使用x,y，因为this 就是当前点（被判断是否在圆内的那个点）
- 编译形式即：this->x , this->y
  (可以解决命名冲突问题，如this->x = x)

**注意：类外函数需要传所需要的点**

### 函数的类内声明，类外实现

- 类内声明：只写声明，不写实现
- 类外实现：真正写函数逻辑，**必须用类名：：** 明确函数的归属，如果没有归属，那么编译器会认为函数为**全局函数** ，而非类的成员函数
- 全局函数：没有this指针，拿不到点（x,y）
## 类的大小

- 对象大小 = 成员变量总和 (考虑对齐) + 虚函数表指针 (如有)。
- 成员函数不占对象空间，存于公共代码段；成员变量单独存储。
- **特殊情况：空类** 
    定义：没有任何成员变量的类。
    大小：**1 字节** (仅用于占位标识对象存在)。

代码验证 (C++)
```C++
class A1 { int _a; void f1(){} }; // sizeof = 4 (仅成员变量)
class A2 { void f2(){} };         // sizeof = 1 (空类规则)
class A3 {};                      // sizeof = 1 (空类规则)
```
### 对齐原则

1. **偏移量的定义** 
    偏移量 = 成员地址 - 结构体（或类）的起始地址，本质是成员相对于整体数据起点的 “距离”，单位为字节。

2. **内存对齐三原则** 
>**第一个成员规则**：结构体 / 类的第一个成员，存储在偏移量为 0 的地址处。
>**其他成员对齐规则**：后续每个成员，都要对齐到「自身大小的整数倍」的地址处（不足的地址空间会用填充字节补齐）。
>**整体大小规则**：结构体 / 类的总大小，必须是其「最大成员对齐数」的整数倍（不足的部分会额外填充字节补齐）。

3. **对齐的核心作用** 
    用少量填充字节的内存开销，换取 CPU 访问内存的效率提升（避免跨内存块多次读取数据）。
# 类与指针

## 传指针与传值的区别

**CCircle* Circle（是一个指针变量）解析：
CCircle：指针指向的类型
星号（\*）是指针符号
Circle是指针变量的名字**

- 指针：直接操作原对象。
- 不复制副本，即不是传”副本“，而是传”地址“，所以如果在函数内更改圆的数据，那么是真的更改。

* 传指针 = 给**家里钥匙**：进家，能随便改家里东西。
- 传值 = 给看**家里的照片**：在照片上乱画，不影响家。
# 友元类

友元类可以访问另一个类的所有成员，但这种关系是单向声明（B是A的友元，但A不是B的友元），并且不可传递
# 内部类

- 一个类定义在另一个类的内部，可以定义在类内的任何区域
- 内部类是一个独立的类，不是组成关系
- 不能通过外部类的对象去调用内部类，因为独立
- **内部类可以通过外部类的对象参数访问外部类的所有成员**，即内部可以访问外部（注意加作用域运算符），但外部不可访问内部，相当于内部类是外部类的友元类

