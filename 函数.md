# 参数传递

|传递方式|核心说明|特点|
|:--|:--|:--|
|传值|按值传递参数，实参的值复制给形参|形参与实参独立，不影响实参，C++ 默认方式|
|传指针|传递地址值，通过指针间接访问|可修改实参，可选参数传递，需要指针语法|
|传引用|按引用传递参数，形参是实参的别名|可修改实参，效率高，语法简洁|

**三种方式对比表：**

|特性|传值|传指针|传引用|
|:--|:--|:--|:--|
|是否修改实参|否|是|是|
|性能开销|较大（复制）|小（地址）|小（引用）|
|安全性|高|中等|高|
|语法复杂度|简单|复杂|简单|
|C++ 推荐|小对象|可选参数|大对象 / 修改|

**信息安全建议：**

传递密钥数据时使用 const 引用，避免不必要的拷贝和修改。
# 缺省参数
#### 问题导入

在实际开发中，函数的某些参数在大多数情况下使用相同的值，但偶尔需要不同的值。是否可以在函数调用时省略这些参数，让编译器自动使用默认值？
#### 缺省参数的作用

- 简化函数调用，减少参数数量
- 提高代码可读性和灵活性
- 减少函数重载数量
#### 代码示例

```C++
// 声明带缺省参数的函数
void print(int ival, int base = 10);

// 调用方式
print(x);      // 使用默认值 10，按十进制输出
print(x, 2);   // 覆盖默认值，按二进制输出
print(x, 16);  // 覆盖默认值，按十六进制输出
```
#### 缺省参数的分类

1. **全缺省参数**：所有参数都有默认值

    ```C++
    void func(int a=1, int b=2, int c=3);
    ```
    
2. **半缺省参数**：部分参数有默认值
    
    ```C++
    void func(int a, int b=2, int c=3);
    ```
#### 缺省参数的规则

1. **从右往左连续设置**
    
    缺省参数必须从右往左连续设置，不能跳跃（比如 `void func(int a=1, int b, int c=3);` 是错误的）。
2. **从左往右传参**
    
    调用函数时，必须按从左到右的顺序依次传递参数，不能跳过前面的参数直接给后面的参数传值。
3. **声明中给出默认值**
    
    当函数的声明和定义分离时，只能在声明中给出缺省值，定义中不能重复设置。
# 函数重载
#### 函数重载的定义

同一作用域内，函数名相同但形参列表不同，编译器根据实参类型自动选择函数版本。

|构成重载|不构成重载|
|:--|:--|
|参数类型不同|返回值类型不同|
|参数数量不同|参数缺省值不同|
|参数顺序不同|函数实现不同|

#### 名字修饰机制

编译器为每个重载函数生成唯一的内部名称，将函数名与参数类型信息编码成唯一标识符。

#### 代码示例

##### 1.参数类型不同

```C++
int add(int a, int b) { return a + b; }
double add(double a, double b) { return a + b; }
string add(string a, string b) { return a + b; }

// 调用示例
add(5, 3);        // 调用 int 版本
add(2.5, 3.7);    // 调用 double 版本
add("Hello", "World"); // 调用 string 版本
```
##### 2. 参数数量不同

```C++
// 2个参数的版本
int add(int a, int b) {
    return a + b;
}

// 3个参数的版本（数量不同，构成重载）
int add(int a, int b, int c) {
    return a + b + c;
}

// 调用示例
add(1, 2);       // 调用 2 参数版本
add(1, 2, 3);   // 调用 3 参数版本
```
##### 3. 参数顺序不同

```C++
// 先 int 后 double
void printInfo(int id, double score) {
    cout << "ID: " << id << ", Score: " << score << endl;
}

// 先 double 后 int（顺序不同，构成重载）
void printInfo(double score, int id) {
    cout << "Score: " << score << ", ID: " << id << endl;
}

// 调用示例
printInfo(101, 95.5);   // 匹配第一个版本
printInfo(95.5, 101);   // 匹配第二个版本
```
# **引用：比指针更安全的别名**

#### 指针的安全性问题

- 空指针：NULL 解引用崩溃
- 野指针：指向已释放内存
- 悬垂指针：指向已销毁对象

#### 引用的定义

引用是变量的别名，不是新变量，而是已存在变量的另一个名字。引用比指针更安全，因为引用必须初始化且不能为空。
#### 语法与基本用法

``` C++
int a = 10;
int& ref = a; // ref 是 a 的引用

ref = 20; // 修改 ref 即修改 a
// cout << a; 输出 20
```
#### 引用的特点

- 必须初始化
- 不能为空
- 一旦绑定不能改变
- 操作引用等于操作原变量
#### 引用与指针的对比

|特性|引用|指针|
|:--|:--|:--|
|初始化要求|必须初始化|可以不初始化|
|是否可以为空|不能为空|可以为 NULL|
|重新绑定|不能重新绑定|可以重新指向|
|安全性|更安全|灵活性更高|
## 常引用与引用的应用场景
#### 常引用的定义与作用

使用 const 关键字修饰引用，声明为常引用。常引用不能通过引用修改原变量的值，提供只读访问权限。
- 定义：`const 类型& 引用名 = 变量;`
- 核心特点：**只能读，不能改**。不能通过这个常引用去修改它绑定的变量。
- 保护数据：防止函数不小心修改了传入的参数，比如加密的密钥、原始明文数据，用 `const` 引用就不会被误改。
- 提高效率：对于字符串、容器这种大对象，用引用传递不用复制一份数据，省内存又省时间。
- 类型转换：可以临时绑定不同类型的变量（比如 `const int& r = 3.14;`，会生成临时变量再绑定）。
#### 常引用示例

```C++
void printData(const string& data) {
    cout << data << endl;
    // data = "new"; // 错误：不能修改
}

string text = "Hello";
printData(text);
```
- 这里的 `const string& data` 就是常引用，它是 `text` 的别名，但不能通过 `data` 修改 `text` 的内容。
- 如果去掉 `const`，就可以在函数里修改 `data`，外面的 `text` 也会跟着变，很容易出问题。
#### 引用的使用场景

1. **函数参数传递：避免拷贝开销**
    对于大对象（字符串、容器、类对象），使用引用传递避免拷贝，提高性能
2. **函数返回值：提高效率**
    返回引用可以避免返回值拷贝，确保返回对象生命周期足够长
3. **操作符重载：实现链式调用**
    操作符重载返回引用支持链式调用，如 `cout << a << b;`
#### 引用作为函数参数示例
```C++
void encryptData(const string& plaintext,
                const string& key,
                string& ciphertext) {
    // 使用引用避免拷贝
    // 通过引用返回结果
}
```
- `const string& plaintext` 和 `const string& key`：用常引用接收明文和密钥，既不拷贝，也防止被误改。
- `string& ciphertext`：用普通引用接收结果变量，函数里修改它，外面的变量也会跟着变，相当于 “输出参数”。
#### 函数返回引用示例
```C++
string& getGlobalConfig() {
    static string config = "default";
    return config; // 安全：静态变量
}

// 错误：返回局部变量引用
string& bad() { string s; return s; }
```
- `string&` 是函数的**返回值类型**，即：该函数不返回变量的拷贝，而返回这个变量的【引用】（别名）
- `static string config` 是静态变量，程序运行期间都存在，返回它的引用是安全的。
-  return config，虽然写的是变量名，但因为函数声明是 `string&`，所以编译器会自动返回 **这个变量的引用**
- `string s` 是局部变量，函数结束就销毁了，返回它的引用会导致未定义行为。

|场景|推荐写法|原因|
|:--|:--|:--|
|函数里只读数据|`const 类型&`|安全不修改，还不用拷贝|
|函数里要修改数据|`类型&`|直接操作原数据，不用拷贝|
|返回静态 / 全局对象|`类型&`|避免拷贝，效率高|
|返回局部对象|直接返回对象（不要用引用）|防止野引用，保证安全|
# 内联函数：消除函数调用开销
#### 问题导入：函数调用的开销

每次函数调用都需要执行压栈、跳转、返回、弹栈等操作。对于频繁调用且短小的函数，这些开销可能成为性能瓶颈。
#### 内联函数的定义与作用

使用 `inline` 关键字声明的函数，建议编译器在编译时将函数代码直接嵌入到调用处。作用：减少函数调用开销，提高程序执行效率。

#### 代码示例

```C++
inline int min(int v1, int v2) {
    return (v1 < v2) ? v1 : v2;
}

// 编译时展开，相当于：
int result = (a < b) ? a : b;
```
#### 优缺点分析

|优点|缺点|
|:--|:--|
|减少调用开销|增加代码体积|
|提高执行效率|可能导致代码膨胀|
|避免性能损耗|增加编译时间|

#### 重要注意事项

- `inline`只是建议，编译器可能不采纳
- 编译器根据函数复杂度决定是否内联
#### 适用场景

✅ 短小频繁调用、简单数学运算

❌ 复杂逻辑循环、递归函数