## 1. vector 到底是什么？

你可以把它理解成：

**一个会自动变大变小的数组，不用你手动管内存。**

对比一下你就懂了：

### 普通数组（你之前用的 `double score[3]`）

- 大小固定，写死 3 就永远只能存 3 个成绩
- 想存 4 个？不行，必须改代码
- 用完还要手动释放，很麻烦

### vector（动态数组）

- **想存几个就存几个**，自动扩容
- 加课程、减课程，**完全不用改代码**
- 不用管内存，自动释放
- 这就是题目要求「**增加课程不用改求平均分函数**」的**最佳写法**

---

## 2. 用在你的学生成绩里，长这样


```C++
// 原来的写法（固定/动态数组，麻烦）
double* m_score;

// 现在的写法（vector，超级简单）
vector<double> m_score;
```

意思就是：

**一个存 double 类型成绩的、可变长的数组**。

---

## 3. 它怎么用？（3 个最常用操作）

### ① 存成绩


```C++
m_score.push_back(85);  // 把 85 放进数组
m_score.push_back(90);  // 再放一个 90
```

### ② 拿成绩


```C++
// 和数组一样用 []
cout << m_score[0];   // 输出 85
```

### ③ 知道有多少个数据


```C++
m_score.size()   // 直接返回元素个数：2
```

---

