# 栈（Stack）与堆（Heap）
## 1. 栈 = 教室临时座位

- 进教室（进函数 / 进 `{}`）**自动给你分配座位**
- 下课走人（出函数 / 出 `}`）**座位立刻被收走**
- 你**不能霸占座位不走**，也不能想什么时候走就什么时候走
- 空间小、速度快、受限制
## 2. 堆 = 外面租的商铺仓库

- 你自己**花钱租一间仓库**（用 `new`）
- 租了之后**没人敢赶你走**，一直给你用
- 什么时候不想用、要关门（卖出货物），**必须自己退租**（用 `delete`）
- 空间超大、你完全自己掌控生命周期
## 1. 普通对象 → 放在【栈】

```C++
Goods g1(10);
```
- 存到 **栈内存**
- 规则：
    只要离开当前 `{ }` 作用域，**系统自动销毁**，自动调用析构
- **不能手动删它**，不能控制什么时候销毁
    👉 缺点：没法实现「想什么时候卖出就什么时候卖出」
## 2. new 创建对象 → 放在【堆】

```C++
Goods* p = new Goods(10);
```
- `new` 就是：**去堆里租一个仓库位置，建一个对象**
- 对象**老老实实待在堆里**，不会自动消失
- 指针 `p` 只是**记一下仓库地址**
## 3. delete → 退租仓库

```C++
delete p;
```
- 把堆里那个对象**手动销毁**
- 立刻调用析构函数
- 归还堆内存
## 栈 和 堆 核心区别

|特性|栈 Stack|堆 Heap|
|---|---|---|
|谁分配内存|系统自动|你用 new 自己申请|
|谁回收内存|出大括号自动销毁|必须自己用 delete 释放|
|生命周期|被 `{}` 锁死，不能自己控制|完全自己掌控，想用多久用多久|
|创建方式|直接定义对象 `Goods g;`|指针 + new `Goods *p = new Goods;`|
|适用场景|临时用一下、用完就扔|需要长期保留、随时手动销毁|
# 指针、new、delete的关系

- **new 负责造对象**（堆里）
- **指针负责记地址、找到对象**
- **delete 负责销毁对象、释放内存**
###  ① `new` 干的事
在**堆**里造一个对象，**返回这个对象的地址**。
### ② 地址必须用**指针**接住

没有指针，你就**找不到堆里那个对象**了，彻底失联。

```C++
// 必须用类指针接收 new 的地址
Goods* p = new Goods(10);
```
### ③ 堆里的对象**不会自己消失**

不收拾它，它一直占内存、赖着不走。
必须用 **delete** 手动销毁它。
### ④ `delete` 只能删「new 出来的、指针指着的对象」
不能随便删普通对象，会崩溃。
# new与delete
## 一、new 是什么

`new` 是 C++ **运算符**，不是函数。
作用：**在堆区动态分配内存，并可构造对象**。

对应配套：
- `new` 分配 + 构造
- `delete` 析构 + 释放内存
## 二、new 分两种用法

### 1. 基础用法 1：new 普通内置类型

```C++
int* p = new int;        // 分配一个int大小堆内存，不初始化
int* q = new int(666);   // 分配 + 初始化为666
```
### 2. 基础用法 2：new 自定义类对象

```C++
Person* per = new Person;      // 无参构造
Person* per2 = new Person(18); // 有参构造，把18传入Person中
```
### 3. 基础用法 3：new 数组

```C++
int* arr = new int[5];   // 堆上开5个int数组
CStudent* students = new CStudent[studentCount];
//动态对象数组，studentCount是对象的个数
```
## 三、执行 `new` 时，底层**自动做 3 件事**

以 `new 类名(参数)` 为例：

1. **调用全局运算符 `operator new()`**
    
    向**堆（Heap）** 申请一块合适大小的原始内存，只分配内存，**还没有对象**。
    
2. **在分配好的内存上 构造对象**
    
    调用**类的构造函数**，初始化成员变量，把这块裸内存变成**合法对象**。
    
3. **返回这块内存的起始地址**
    
    必须用**同类型指针**接收，否则内存丢失（内存泄漏）。
    
## 四、对应的 delete 底层做 2 件事

1. **先调用对象的析构函数**
    
    清理对象内部资源。
2. **调用 `operator delete()`**
    
    把堆内存归还给系统。

> 重点：
> 
> 普通局部对象（栈上）：系统自动分配、自动构造、自动析构、自动释放
> 
> new 对象（堆上）：**内存、构造、析构、释放 全都要你手动管控**
## 五、区分两个容易混淆的概念

### 1. `new` 运算符（平时写的这个）

就是 `new Person(18)`

**不可重写整体流程**，固定三步：分配内存 → 构造对象 → 返回地址。

### 2. `operator new` 库函数

是底层负责**只申请内存**的函数，

只分配裸内存，**不调用构造函数**，可以自己重载改写。

即：

**平时写的 new 运算符，内部默认调用了 operator new 来拿内存**。