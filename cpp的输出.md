#date：2026/4/13
# boolalpha:

- `boolalpha` 是 C++ 的 **I/O 流操纵符**，用来控制 **布尔类型（bool）的输出格式**。
- **目的**：让 `IsInCircle` 返回的 `bool` 结果，**不是** 显示成冷冰冰的 `1` 或 `0`，而是**显示成人类易读的 `true` 或 `false`**。
- 一旦开启，全局生效:`boolalpha` 是 “粘性” 的（Sticky）。你写了一次 `cout << boolalpha;`，**之后所有的 `bool` 输出**都会变成 `true/false`，直到用 `noboolalpha` 关闭。

```cpp
//使用boolalpha
cout << boolalpha; // 开启 
cout << true << endl; // 输出 true 
cout << false << endl;// 输出 false 
cout << noboolalpha; // 关闭 
cout << true << endl; // 恢复输出 1
```


>代码块前后有三个反引号，第一行反引号后面加上代码语言。

# 字符串

- **char 数组存字符串，必须以 \0 结尾** 
>比如字符串 "张三",实际在**内存** 里存的是：张 三 \0，
- **\0 是字符串结束标志，没有它，程序不知道字符串到哪里结束，会乱码、崩溃** 
## strcpy = string copy（字符串拷贝）

**作用**：
- 把 n 里的所有内容，包括最后的 \0，完整复制到 name 里
>n = "张三",内存里是：张 三 \0，只要写 "张三"，系统就会自动在后面藏一个 \0，看不见，但它一定存在
 执行 strcpy(name, n) 后：name 里也变成了：张 三 \0
 即：
 张 → name[0]
 三 → name[1]
 \0 → name[2]
## strlen() = 字符串长度

>不带 \0，不加 1
>char \*n = "张三";
>strlen(n) = 2（不算 \0）
 申请内存时 = strlen () + 1，因为要存结束符 \0
# const

- **const 放后面**：常成员函数，**管对象**（不修改对象）
- **const 放前面**：修饰返回值，**管返回值**（返回的东西不能改）
```C++
// 正确：const 在后面 → 常成员函数（不修改对象）
void showdate() const; 

// 错误理解：const 在前面 → 修饰返回值，不是常函数
const void showdate(); 
```
常量对象只能调用 不修改它的函数 → 也就是后面带 const 的常成员函数
## const与指针

三种情况（以 `int` 为例）
### 1. 指向**常量**的指针（`const int* p`）
```C++
const int* p;
```
- 👉 修饰的是 `int`，也就是**指针指向的内容**
- **含义**：指针指向的那个值，不能通过这个指针修改
- **特点**：
    - 指针本身可以变（可以指向别的地址）
    - 指向的内容不能通过 `p` 改（`*p = 100;` 会报错）
- 例子：
```C++
int a = 10, b = 20;
const int* p = &a;

p = &b;      // ✅ 可以：指针本身变了，指向b
*p = 100;    // ❌ 报错：不能通过p修改指向的值
```
### 2. **指针本身**是常量（`int* const p`）
```C++
int* const p;
```
- 👉 修饰的是 `p`，也就是**指针本身**
- **含义**：指针存的地址值不能变（一旦指向某个变量，就再也不能改指向）
- **特点**：
    - 指针本身不能变（不能再指向别的地址）
    - 指向的内容可以改（`*p = 100;` 是允许的）
- 例子：
```C++
int a = 10, b = 20;
int* const p = &a;

p = &b;      // ❌ 报错：指针本身是const，不能改指向
*p = 100;    // ✅ 可以：可以修改指向的内容
```
### 3. 指向**常量**的**常量指针**（`const int* const p`）
```C++
const int* const p;
```
- 👉 两个 `const` 分别修饰了 `int` 和 `p`
- **含义**：指针本身不能变，指向的内容也不能变
- **特点**：完全锁死，既不能改指向，也不能改内容
- 例子：
```C++
int a = 10, b = 20;
const int* const p = &a;

p = &b;      // ❌ 报错：指针本身是const
*p = 100;    // ❌ 报错：指向的内容是const
```

**读的时候，从右往左读**：
    
    1. const int* p → 读作 `p` 是一个指针，指向一个 `const int`（指向的内容不能改）
    2. int* const p → 读作 `p` 是一个 `const` 指针，指向 `int`（指针本身不能改）
    3. const int* const p → 读作 `p` 是一个 `const` 指针，指向一个 `const int`（两者都不能改）

**注意**：

下面这两种写法是完全一样的

```C++
const int* p;
int const* p;
```

它们都是 “指向常量的指针”，修饰的都是指向的内容。
只有 `const` 写在 `*` 后面，才是修饰指针本身。