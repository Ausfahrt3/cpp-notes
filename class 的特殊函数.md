#date：2026/4/15
#lable：实验3-4

**空类并不空（什么都没有的类占空间为1），编译器会自动生成6个默认的成员函数**，用户显示定义之后就不会再自动生成。

>**特点** ：
>- 名字**必须跟类名一样**（或前面加～）
>- **创建 / 销毁对象时自动调用**
>- 不能手动 `c1.CPoint()这么调用(即不能像成员函数那样调用)
>- 负责**对象的出生、复制、死亡**


# 构造函数：

**对象创建时自动初始化对象，无返回值，支持重载** 
## 默认构造函数（可以被无参调用的构造函数）：

- 无参构造函数：无需传递参数；
- 全缺省构造(即每个参数都写了初始化，如下文的Date类就是全缺省，不用再额外写谁等于谁)：所有参数都有默认值；
- 编译器自动生成的函数，**不写时编译器自动生成，但其内置成员未初始化会有随机值带来的错误**；
**默认构造函数只能有一个，即无参构造和全缺省构造不能同时存在，因为二者在调用时都可以无参调用，否则会导致调用歧义**。
### 类调用对象
```C++
class CTeacher
{
    CStudent stu;   // 就是这里！！！
};
```
**这句话的意思是：** 
> 创建老师的时候，**必须同时创建学生 stu**，而且**不能传参数**
> 因为这里没办法写参数，这是无参创建对象，所以编译器会去寻找**无参构造函数** ，而当CStudent的类中含有带参构造函数时，编译器不会再自动生成默认构造函数，此时，编译器既找不到无参构造函数，也不能自动生成这类构造函数时，编译器就会报错

**解决方案**：
- 多写一个空参构造函数
```C++
class CStudent
{
public:
    // 👇 加上这一行！无参构造 = 默认构造函数
    CStudent() {}  

    // 你原来的带参构造
    CStudent(char *n, int a); 
};
```
- 在 CTeacher 里**手动初始化 stu**（最标准）
    不改 CStudent，只改 Teacher 的构造函数，**用初始化列表**：
```C++
CTeacher::CTeacher(char *tn, int ta)
    : stu("临时学生", 0)  // 手动给stu传参，创建它
{
    // ...原来的代码
}
```
**初始化**：可通过初始化列表完成成员的初始化，**部分必须通过初始化列表进行初始化**。
```C++
Date(int year = 2026,int month = 4, int day = 26)
:_year(year)
,_month(month)
,_day(day)
//_year等是成员函数，括号内是形参，意思是将括号内的形参赋值给成员函数，完成初始化
{
//初始化列表完成成员的初始化
}
```
### 静态成员变量

可在构造函数中操作静态成员变量，如统计+1，因为是静态，所有对象共用这一个计数器，才能统计总数，而且构造函数在创建对象时完成初始化，所以能够+1。
```C++
CStudent::CStudent(char *n, int a):age(a) 
{ 
    int nLen = strlen(n); 
    name = new char[nLen+1]; 
    strcpy(name, n);
     nTotalObj++; 
}
```
## 委托构造函数：

**一个构造函数调用另一个构造函数完成初始化，避免代码重复**。
```C++
class Info{
    string name;
    int age;
public:
    Info(string n, int a)
    :name(n),age(a){}
    
    Info(string n)
    :Info(n,0){}
    
    Info()
    :Info("",0){}
}
```
*以上三个函数是普通的重载函数*

- 委托构造**必须写在初始化列表里**，写在函数体里没用；
- 不能形成互相调用的循环链，否则会无限递归导致程序崩溃。
### 必须在初始化列表中完成初始化的情况

- `const` 成员变量
>初始化列表的作用，就是**在成员变量出生的瞬间就给它赋值**，这属于 “初始化”，而不是 “修改”,如果在函数体中再写类似 year = y等地语句，只是赋值操作，然而由于const 的原因，并不会 修改。
- const变量
- 无默认构造函数的自定义类型：成员对象无默认构造函数时，需要在构造函数中进行初始化列表进行初始化
因此，最好都在初始化列表中进行初始化
注意⚠️
- 初始化顺序是成员变量在类中声明的次序，与列表中的书写顺序无关
- 初始化列表中顺序最好与声明次序
- 禁止用一个变量初始化另一个变量

# 析构函数：

**格式为~Date（），在对象生命周期结束时自动调用，无需手动调用，完成对象清理，不可重载，无参数,一个类只能有一个析构函数**。

有动态内存申请时，必须自定义析构函数释放资源

*stack 栈（先进后出原则）*

```C++
class Stack 
{ 
public: 
// 构造函数：创建对象时自动调用，申请内存 
Stack(int capacity = 10) 
    : _data(new int[capacity])
    , _capacity(capacity)
    , _size(0) {} 
// 析构函数：对象销毁时自动调用，释放内存 
~Stack() { 
    if (_data) { 
    delete[] _data; // 释放构造函数申请的内存 
    _data = nullptr; } 
} 
private: 
    int* _data; // 指向动态申请的数组 
    int _capacity; // 数组容量 
    int _size; // 数组当前大小 
};
```

1. **new 是什么**
    
    new 是 C++ 关键字，是**主动向操作系统申请堆内存**的操作，不是符号。
    普通变量内存由系统自动分配释放；
    new 申请的堆内存不会自动回收，需要手动释放。
    
2. **new 两种用法 & [] 的作用** 

- `new 类型`：申请**单个变量**的内存，只存一个数据。
- `new 类型[个数]`：加方括号代表申请**连续的多块内存**，也就是**动态数组** （动态 = **运行时才确定大小，不是写死的**），中括号内可以写变量，灵活控制数组长度。
    动态数组最重要的 3 个特点：
    - **大小可以是变量**（不是必须写死数字）
    - **内存分配在堆区**
    - **用完必须用 delete [] 释放**
- **释放规则（必须配对）**
    - 单个内存：`new` 对应 `delete`
    - 数组内存：`new[]` 对应 `delete[]`
    
    配对错误会造成程序崩溃、内存泄漏。

4. **结合代码理解**
    
    `new int[capacity]`：申请 `capacity` 个整型大小的连续数组空间，再把空间地址交给指针保存,初始化数组中的赋值，\_data(new int[capacity]),意思就是，把申请到的一串内存房间的地址，写到\_date这个小纸条上（`name` 是一个 **指针变量**，它只存地址，不存名字本身，相当于**name = 一个小纸条**，小纸条上写的是：**名字存放在内存的哪个房间号**）。
    
4. **构造函数形参与初始化的区别** 
    
    构造函数形参只有一个，仅作为**外部传入的数值**；
    
    初始化列表可以自行完成：开辟动态内存、保存传入参数、设置固定初始值，不需要每个成员都对应一个形参。

**关于`if(_Date)`**：
1. `_data` 是指针：

- 开过内存：存着有效地址 → **不为空**
- 没开内存：等于 `nullptr`（空）

1. `if(_data)` 等价于：**判断 \_data 是不是空指针**

- 不为空（有内存）→ 条件成立，执行 `delete[]` 释放内存
- 是空（无内存）→ 条件不成立，直接跳过，不执行删除

3. 加 `if` 的目的：
    
    防止**空指针**强行释放内存，导致程序崩溃，起到安全保护作用。
### 静态成员变量

与上文构造函数类似，在main函数结束后，执行析构函数释放内存，可以执行-1操作。
main 里面所有代码从上到下跑完 → main 结束
main 一结束，里面的局部对象立刻自动销毁
打印语句在销毁之前执行，所以能看到真实对象个数
```C++
CStudent::~CStudent()
 { 
     delete[] name; 
     nTotalObj--; 
}
```
# 拷贝构造函数：

- 单个形参为本类类型对象的引用
- 用已存在对象创建新对象时自动调用
- 属于构造函数的重载形式

#### 语法格式

```C++
class 类名 {
public:
    类名(const 类名& 对象名){
        // 拷贝构造函数实现
    }
};
```
##### 四大核心特性

1. 是构造函数的重载形式，函数名与类名相同
2. 参数必须使用引用，避免无穷递归调用
3. 未定义时编译器自动生成默认拷贝构造函数
4. 编译器生成的拷贝构造函数只完成浅拷贝

当类中包含**指针成员或动态资源**时，必须自定义拷贝构造函数实现深拷贝

- 包含动态数组的 Stack 类
- 包含文件句柄的 File 类
- 包含网络连接的 Socket 类

|浅拷贝 Shallow Copy|深拷贝 Deep Copy|
|:--|:--|
|只复制指针值|复制指针指向的内容|
|不复制指针指向的内容|申请新的内存空间|
|多个对象共享内存|每个对象独立内存|
|析构时重复释放 → 崩溃|析构时安全释放|

**Stack类的深拷贝**：
```C++
class Stack {
public:
    Stack(int cap = 10)
        : _data(new int[cap]), _capacity(cap), _size(0) {}

    Stack(const Stack& s) { // 深拷贝构造
        // 1. 申请新内存
        _data = new int[s._capacity];
        // 2. 复制数据
        memcpy(_data, s._data, s._size*sizeof(int));
        // 3. 复制成员变量
        _capacity = s._capacity;
        _size = s._size;
    }

    ~Stack() { delete[] _data; }
private:
    int* _data;
    int _capacity;
    int _size;
};
```
**注意**：
1. `memcpy` 是 C/C++ 里的一个标准库函数，用来**直接按字节复制内存数据**。
    它的标准格式是：
```C++
memcpy(目标地址, 源地址, 要复制的字节数);
```
2. s.\_size * sizeof(int)
    意为：总字节数 = 元素个数 × 每个元素的字节数


# const成员函数



# 赋值运算符重载函数


# 取地址操作符重载

